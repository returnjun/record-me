spring:
  config:
    name: record-me-app
  profiles:
    active: dev

llm:
  deepseek:
    model: deepseek-v4-flash
    base-url: https://api.deepseek.com
    api-key: ${DEEPSEEK_API_KEY:}
    timeout: 120
    max-retries: 3
    retry-sleep: 2.0
