<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.daoha.infrastructure.dao.ICycleRecordDao">

    <resultMap id="CycleRecordMap" type="top.daoha.infrastructure.dao.po.CycleRecord">
        <id column="cycle_id" property="cycleId"/>
        <result column="user_id" property="userId"/>
        <result column="start_date" property="startDate"/>
        <result column="end_date" property="endDate"/>
        <result column="is_active" property="isActive"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="top.daoha.infrastructure.dao.po.CycleRecord" useGeneratedKeys="true" keyProperty="cycleId">
        INSERT INTO cycle_record(user_id, start_date, end_date, is_active)
        VALUES(#{userId}, #{startDate}, #{endDate}, #{isActive})
    </insert>

    <select id="selectById" parameterType="java.lang.Long" resultMap="CycleRecordMap">
        SELECT cycle_id, user_id, start_date, end_date, is_active,
               is_deleted, create_time, update_time
        FROM cycle_record
        WHERE cycle_id = #{cycleId} AND is_deleted = 0
    </select>

    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="CycleRecordMap">
        SELECT cycle_id, user_id, start_date, end_date, is_active,
               is_deleted, create_time, update_time
        FROM cycle_record
        WHERE user_id = #{userId} AND is_deleted = 0
        ORDER BY start_date DESC
    </select>

    <select id="selectActiveByUserId" parameterType="java.lang.Long" resultMap="CycleRecordMap">
        SELECT cycle_id, user_id, start_date, end_date, is_active,
               is_deleted, create_time, update_time
        FROM cycle_record
        WHERE user_id = #{userId} AND is_active = 1 AND is_deleted = 0
    </select>

    <update id="updateById" parameterType="top.daoha.infrastructure.dao.po.CycleRecord">
        UPDATE cycle_record
        <set>
            <if test="startDate != null">start_date = #{startDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
        </set>
        WHERE cycle_id = #{cycleId} AND is_deleted = 0
    </update>

    <update id="deleteById" parameterType="java.lang.Long">
        UPDATE cycle_record SET is_deleted = 1 WHERE cycle_id = #{cycleId}
    </update>

    <update id="overByUserId" parameterType="java.lang.Long">
        UPDATE cycle_record SET end_date = NOW()
        WHERE user_id = #{userId} AND is_active = 1 AND is_deleted = 0
    </update>

    <update id="closeByUserId" parameterType="java.lang.Long">
        UPDATE cycle_record SET is_active = 0
        WHERE user_id = #{userId} AND is_active = 1 AND is_deleted = 0
    </update>

    <select id="selectCompletedByUserId" parameterType="java.lang.Long" resultMap="CycleRecordMap">
        SELECT cycle_id, user_id, start_date, end_date, is_active,
               is_deleted, create_time, update_time
        FROM cycle_record
        WHERE user_id = #{userId} AND end_date IS NOT NULL AND is_deleted = 0
        ORDER BY start_date ASC
    </select>

    <select id="countByUserId" parameterType="java.lang.Long" resultType="int">
        SELECT COUNT(1) FROM cycle_record
        WHERE user_id = #{userId} AND is_deleted = 0
    </select>

    <select id="selectByUserIdPage" resultMap="CycleRecordMap">
        SELECT cycle_id, user_id, start_date, end_date, is_active,
               is_deleted, create_time, update_time
        FROM cycle_record
        WHERE user_id = #{userId} AND is_deleted = 0
        ORDER BY start_date DESC
        LIMIT #{offset}, #{limit}
    </select>

</mapper>
